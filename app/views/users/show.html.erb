<div class="show-item">
  <%= render @user %>
</div>

<nav class="page-nav">
  <% if current_user == @user %>
    <%= link_to t('views.users.show.edit_info', name: User.model_name.human.downcase), edit_user_path(@user) %> |
  <% end %>
  <%= link_to t('views.common.back', name: i18n_pluralize(User.model_name.human.downcase)), users_path %>
</nav>
